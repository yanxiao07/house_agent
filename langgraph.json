{
  "$schema": "https://langgra.ph/schema.json",
  "dependencies": ["."],
  "graphs": {
    "house_agent": "agent.graph:graph",
    "recommend_agent": "agent.recommend:recommend_graph",
    "reserve_agent": "agent.reserve:reserve_graph",
    "extend_agent": "agent.extend:extend_graph",
    "browse_agent": "agent.browse:browse_graph"
  },
  "env": ".env",
  "image_distro": "wolfi"
}
